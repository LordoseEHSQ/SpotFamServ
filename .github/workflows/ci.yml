name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  # Opt-in auf Node24 fuer JS-Actions (Node20 wird ab 2026-06-16 abgekuendigt).
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
  APP_ENV: test
  APP_SECRET: ci-dummy-secret
  DATABASE_URL: "postgresql://spotfam:spotfam@127.0.0.1:5432/spotfam?serverVersion=17&charset=utf8"
  SPOTIFY_REDIRECT_URI: "http://localhost/api/v1/spotify/callback"
  FRONTEND_URL: "http://localhost:5173"
  READER_API_KEY: "ci-dummy"

jobs:
  backend:
    name: Backend (PHP ${{ matrix.php }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        php: ['8.4', '8.5']
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          extensions: intl, pdo_pgsql, sodium
          coverage: none
          tools: composer:v2

      - name: Provide .env (für Symfony-Runtime / auto-scripts)
        run: cp .env.example .env

      - name: Validate composer files
        run: composer validate --no-check-publish

      - name: Cache Composer
        uses: actions/cache@v4
        with:
          path: ~/.composer/cache
          key: composer-${{ matrix.php }}-${{ hashFiles('backend/composer.lock') }}
          restore-keys: composer-${{ matrix.php }}-

      - name: Install dependencies
        run: composer install --no-interaction --no-progress --prefer-dist

      - name: PHPStan (Level 8)
        run: php vendor/bin/phpstan analyse --no-progress --memory-limit=1G

      - name: PHPUnit
        run: php vendor/bin/phpunit

      - name: Security audit (Composer-Abhängigkeiten)
        run: composer audit

  frontend:
    name: Frontend (Node 22)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: frontend/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # Hinweis: 'pnpm lint' ist bewusst NICHT aktiv – ESLint ist im Projekt
      # weder als Dependency noch per Config eingerichtet. Erst ESLint einführen,
      # dann hier einen Lint-Step ergänzen.
      - name: Build
        run: pnpm build

      - name: Test
        run: pnpm exec vitest run --passWithNoTests

  firmware:
    name: Firmware Compile (ESP32)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: firmware/spotfam_reader
    steps:
      - uses: actions/checkout@v4

      - name: Setup arduino-cli
        uses: arduino/setup-arduino-cli@v2

      - name: Install ESP32 core and libraries
        run: |
          arduino-cli config init --overwrite
          arduino-cli core update-index
          arduino-cli core install esp32:esp32@3.3.8
          arduino-cli lib install ArduinoJson@7.4.3
          arduino-cli lib install MFRC522@1.4.12
          arduino-cli lib install "Adafruit PN532"@1.3.4
          arduino-cli lib install "Adafruit BusIO"@1.17.4

      - name: Provide CI firmware secrets
        run: cp secrets.h.example secrets.h

      - name: Compile SpotFam reader firmware
        run: arduino-cli compile --fqbn esp32:esp32:esp32 .

      - name: Compile PN532 HW-0 probe (Diagnose)
        working-directory: firmware/spotfam_pn532_probe
        run: arduino-cli compile --fqbn esp32:esp32:esp32 .

  flash-agent:
    name: Flash-Agent (Python)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: firmware/flash_agent
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: py_compile
        run: python -m py_compile flash_agent/*.py tests/*.py

      - name: pytest
        run: python -m pytest tests -q

  api-drift:
    name: API-Drift (oasdiff)
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.5'
          extensions: intl, pdo_pgsql, sodium
          coverage: none
          tools: composer:v2

      - name: Provide .env (für Symfony-Runtime)
        working-directory: backend
        run: cp .env.example .env

      - name: Install dependencies
        working-directory: backend
        run: composer install --no-interaction --no-progress --prefer-dist

      - name: Generate current OpenAPI spec (revision)
        working-directory: backend
        run: php bin/console nelmio:apidoc:dump --format=yaml > openapi.yaml

      - name: Extract base OpenAPI spec
        run: |
          git fetch --no-tags --depth=1 origin "${{ github.base_ref }}"
          if git cat-file -e "origin/${{ github.base_ref }}:backend/openapi.yaml" 2>/dev/null; then
            git show "origin/${{ github.base_ref }}:backend/openapi.yaml" > base-openapi.yaml
            echo "has_baseline=true" >> "$GITHUB_OUTPUT"
          else
            echo "Keine OpenAPI-Baseline auf '${{ github.base_ref }}' – Drift-Check wird übersprungen."
            cp backend/openapi.yaml base-openapi.yaml
            echo "has_baseline=false" >> "$GITHUB_OUTPUT"
          fi
        id: base

      - name: Detect breaking API changes
        uses: oasdiff/oasdiff-action/breaking@v0.0.49
        with:
          base: base-openapi.yaml
          revision: backend/openapi.yaml
          fail-on: ERR
          # Eng begrenzte, dokumentierte Ausnahmen (siehe Datei). Aktuell nur die
          # UUID-pattern-Verschaerfung an {jobId} (kein realer Vertragsbruch).
          err-ignore: backend/.oasdiff-err-ignore

  security:
    name: Security Scan (Trivy)
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: actions/checkout@v4

      # Pin auf v0.36.0 per SHA (Tag-Refs sind hier v-praefixiert/uneindeutig).
      # exit-code: '0' = report-only, blockiert den PR (noch) nicht; auf '1' setzen,
      # um bei CRITICAL/HIGH-Funden hart zu gaten.
      - name: Trivy filesystem scan
        uses: aquasecurity/trivy-action@a9c7b0f06e461e9d4b4d1711f154ee024b8d7ab8 # v0.36.0
        with:
          scan-type: fs
          scan-ref: .
          scanners: vuln,secret
          severity: CRITICAL,HIGH
          ignore-unfixed: true
          exit-code: '0'
          skip-dirs: backend/vendor,frontend/node_modules
